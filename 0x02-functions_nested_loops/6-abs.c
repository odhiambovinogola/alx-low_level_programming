#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @x: x is an integer
 *
 * Return: integer
 */

int _abs(int x)
{
	if (x >= 0)
	{
		return (x);
	}
	else
	{
		return (-x);
	}
}
